---
"@wraithwalker/extension": patch
---

Add ESLint 9 flat config with `eslint-plugin-react-hooks` and a dedicated GitHub Actions workflow so Rules of Hooks violations in the React UI are caught automatically.
