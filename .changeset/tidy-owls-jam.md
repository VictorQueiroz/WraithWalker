---
"@wraithwalker/extension": minor
---

Improve the extension's daily capture flow with clearer readiness guidance in the popup and Settings, canonicalize duplicate origins at extension config boundaries, prevent duplicate whitelist writes, and ship the built extension with React's production runtime.
