---
"@wraithwalker/core": patch
"@wraithwalker/extension": patch
"@wraithwalker/mcp-server": minor
---

Stream large fixture writes outside JSON payload limits, filter replay headers
before CDP fulfillment, and add MCP navigation checkpoints for durable fixture
positioning.
