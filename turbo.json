{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**", "out/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "typecheck": {},
    "generate:icons": {
      "outputs": ["static/assets/icons/**"]
    }
  }
}
